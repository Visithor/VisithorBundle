imports:
    - { resource: security.yml }

framework:
    test: ~
    secret: fhklsdhkj
    form:
        enabled: false
    session:
        storage_id: session.storage.mock_file
    profiler:
        collect: false
    router:
        resource: "%kernel.root_dir%/routing.yml"
        strict_requirements: ~
